[pytest]
testpaths = tests
log_cli = true
log_cli_level = INFO
markers =
    e2e: browser-driven end-to-end UI tests (slower, real Chrome instance)
    api: direct API tests (fast, no browser)
addopts = -ra --strict-markers
